import styled from '@emotion/styled';

export const StyledBanner = styled.div`
  position: relative;

  & .buttonContainer {
    position: absolute;
    right: 5%;
    bottom: 5%;
    width: 150px;
  }
`;
